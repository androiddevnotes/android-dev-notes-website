---
title: "Schedule Local Push Notification that works even when app is in background with AlarmManager, BroadcastReceiver and NotificationManager"
sidebar_label: "Local Push Notification - Background"
slug: "/omi-72/LocalPushNotification"
description: "Schedule Local Push Notification that works even when app is in background with AlarmManager, BroadcastReceiver and NotificationManager"
tags: [Android, Kotlin, Push Notification, AlarmManager, BroadcastReceiver, NotificationManager]
keywords: [Android, Kotlin, Push Notification, AlarmManager, BroadcastReceiver, NotificationManager, Android Dev Notes]
image: "/static/img/androiddevnotes-social.png"
---

import ImageDisplay from '/src/utils/ImageDisplay';

# Schedule Local Push Notification that works even when app is in background with AlarmManager, BroadcastReceiver and NotificationManager

## Overview

The app demos a specific functionality of how to schedule local push notifications that work even when the app is in the background. The app uses the AlarmManager, BroadcastReceiver and NotificationManager to schedule and display the notifications.

## Tech Stack

- AlarmManager
- BroadcastReceiver
- NotificationManager
- Calendar
- ViewBinding

## Screenshots

export const imageUrls = [
  'https://storage.androiddevnotes.com/api/raw/?path=/assets/2/4x1.png',
  'https://storage.androiddevnotes.com/api/raw/?path=/assets/2/5x1.png',
];

<ImageDisplay imageUrls={imageUrls} />

## Code

You can find the complete source code for this project on [GitHub](https://github.com/omi-72/LocalPushNotification).

## Build Instructions

To get started with this project, follow these steps:

1. Clone the repository to your local machine using the following command:

 ```
 git clone https://github.com/omi-72/LocalPushNotification
    ```
2. Open the project in Android Studio.

3. Build and run the project.

## Summary

App works as expected. Notification shows up even when the app is in background.

Try it out, run the code!

🐣
